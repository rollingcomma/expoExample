import React from 'react';
import { StyleSheet, Text } from 'react-native';
import Screen from '../components/Screen';

export default Feed = ({navigation, route}) => {
  return(
    <Screen>
      <Text>Placeholder for Feed</Text>
    </Screen>
  )
}

const styles = StyleSheet.create({
});