import React from 'react';
import { Text, View } from 'react-native';

export default Profile = () => {
  return(
    <View>
      <Text> Placeholder for profile from other user's view </Text>
    </View>
  )
}